# Netlify: serve the Uptime page at the root of uptime.yungdice.com.
# Only "/" is rewritten, so /_next/* assets and /images/* still load normally.
https://uptime.yungdice.com/   /uptime.html   200!
http://uptime.yungdice.com/    /uptime.html   200!
